import type { Metadata } from 'next';
import { Geist, Geist_Mono } from 'next/font/google';
import './globals.css';

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
});

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
});

export const metadata: Metadata = {
  title: 'EcoCarbon | AI-Powered Carbon Footprint Assistant',
  description: 'Track your carbon emissions, receive personalized reduction plans, challenge yourself with sustainability goals, and chat with EcoBot to lead a greener life.',
  keywords: ['sustainability', 'carbon footprint', 'eco assistant', 'carbon calculator', 'green habits', 'climate change'],
  authors: [{ name: 'EcoCarbon Team' }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>
      <body className="antialiased">
        {children}
      </body>
    </html>
  );
}
